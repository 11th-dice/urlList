import {indexRoutes} from 'chai';

describe('route get / index 200', function () {
	it('modelに正常に値が入っている');
});

describe('route post / index 200', function () {
	it('DBがアップデートされている');
});

describe('route get /delete index 200', function () {
	it('削除されている');
});

